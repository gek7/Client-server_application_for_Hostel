<Page x:Class="Client_serv.Pages.MultiPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client_serv.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MultiPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Width="20" Grid.Row="0" Grid.Column="2" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Image Source="/Images/close2.png"/>
        </Button>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" Width="3" Background="Black"  HorizontalAlignment="Center"  />
            <Frame  x:Name="dg" Grid.Column="0" NavigationUIVisibility="Hidden" />
            <Frame Grid.Column="2" NavigationUIVisibility="Hidden" />
        </Grid>
    </Grid>
</Page>
